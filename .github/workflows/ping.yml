name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 1 * * *'

  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Website
        run: curl https://forex-pd.vercel.app/api/ping
